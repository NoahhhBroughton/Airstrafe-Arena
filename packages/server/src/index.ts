// Placeholder. Not started until Phase 3 (docs/ROADMAP.md) - the authoritative fixed-tick
// simulation loop, WebSocket handling, and reconciliation snapshot logic described in
// docs/ARCHITECTURE.md belong here. Do not build this out before Phase 1 movement is tuned.

import { TICK_RATE } from "@airstrafe-arena/shared";

console.log(`server placeholder - target tick rate ${TICK_RATE}Hz. Not implemented yet (Phase 3).`);
